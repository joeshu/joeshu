# 需求实施计划

- [x] 1. 完成统一日程流基础能力
  - 接入 `TodoItem.scheduledStart` 和 `scheduledEnd` 到日程时间线
  - 统一日历事件与排期待办的排序、数量和完成进度
  - 保留已完成排期待办并支持完成状态切换
  - 对应 Requirement 5.1-5.4、Requirement 6.2-6.3

- [x] 2. 完善跨日排期待办展示和当前时间标记
  - 按完整排期日期区间覆盖月视图、周视图和日程视图
  - 展示跨日首日、中间日和末日的时间语义
  - 在今天的执行时间线中插入当前时间标记
  - 对应 Requirement 6.1、Requirement 6.4、Requirement 7.3

- [x] 3. 增加日历中的排期待办编辑入口
  - 在日程时间线和周视图待办上下文菜单中增加编辑排期入口
  - 复用 `TaskScheduleSheet` 修改开始时间、预计时长和结束时间
  - 复用现有 `modelContext.save()` 错误处理路径
  - 对应 Requirement 7.1-7.2

- [x] 4. 建立周视图小时网格和拖拽改期接口
  - 将 `WeekCalendarCard` 从日期列布局扩展为可定位的日期列与小时槽布局
  - 定义事件和待办拖拽项的统一时间定位模型
  - 为拖拽目标提供日期、小时和分钟的换算接口
  - 保持 iPhone 单列和 iPad 宽屏布局的可读性
  - 对应设计文档中的周视图规划层和现有布局正确性属性

- [ ]* 4.1 为拖拽时间换算编写属性测试
  - 验证任意合法日期、小时和分钟都能映射回同一时间槽
  - 验证跨天拖拽保持开始时间早于结束时间
  - 关联设计正确性属性：日期计算失败时沿用当前有效日期

- [x] 5. 实现日历事件和排期待办拖拽改期
  - 为 `CalendarEvent` 提供按目标日期和时间更新开始、结束时间的操作
  - 为 `TodoItem` 提供按目标日期和时间更新开始、结束时间的操作
  - 保持事件时长和待办预计时长不变
  - 保存失败时恢复拖拽前的时间值并显示错误提示
  - 对应 Requirement 7.2、设计文档 Error Handling

- [ ]* 5.1 为拖拽改期保存流程编写单元测试
  - 覆盖事件改期、待办改期、跨日改期和保存失败回滚
  - 验证完成状态与排期时间互不覆盖

- [x] 6. 增加全天事件和全天待办区域
  - 为日历展示层区分全天项、跨日项和具体时间项
  - 在月视图、周视图和日程视图顶部增加独立全天区域
  - 为全天项提供新增、编辑、完成和清除排期入口
  - 设计模型字段扩展和 SwiftData 迁移路径，避免直接破坏已有数据
  - 对应 Requirement 4.3、Requirement 6.1、Requirement 7.3

- [ ]* 6.1 为全天和跨日规则编写属性测试
  - 验证全天项不会进入具体小时排序区域
  - 验证跨日项覆盖开始日期、结束日期和中间日期
  - 验证结束时间早于开始时间时无法保存

- [x] 7. 实现重复日程规则和实例展示
  - 定义重复频率、间隔、结束条件和例外日期的数据结构
  - 为日历事件和排期待办生成指定日期范围内的展示实例
  - 支持单次修改实例与修改整个重复序列的操作边界
  - 为月视图、周视图和日程视图复用同一重复实例查询逻辑
  - 对应商业化日历扩展方向和现有 SwiftData 架构

- [ ]* 7.1 为重复规则生成编写属性测试
  - 验证每日、每周和间隔重复规则的日期序列单调递增
  - 验证结束日期和次数限制不会生成越界实例
  - 验证例外日期不会出现在最终实例集合中

- [x] 8. 实现提醒配置和本地通知调度
  - 增加事件和待办的提醒时间配置
  - 使用统一调度服务创建、更新和取消本地通知
  - 处理权限拒绝、通知标识冲突和已过期提醒
  - 在编辑、完成、清除排期和删除事件时同步通知状态
  - 对应商业化日历提醒能力和现有保存错误处理约定

- [ ]* 8.1 为提醒调度编写单元测试
  - 覆盖提前提醒、重复提醒、取消提醒和过期提醒
  - 验证保存失败时通知状态不会领先于持久化数据

- [x] 9. 增加日历筛选和冲突检测
  - 支持按事件分类、待办来源、完成状态和全天状态筛选
  - 计算事件与排期待办的时间重叠区间
  - 在周视图和日程视图中显示冲突提示和容量警告
  - 保持筛选状态不改变月份和选中日期
  - 对应 Requirement 1.2、设计文档视图状态正确性属性

- [ ]* 9.1 为筛选和冲突计算编写属性测试
  - 验证筛选结果只包含满足全部活动筛选条件的项目
  - 验证不重叠区间不会产生冲突
  - 验证跨日区间冲突计算覆盖所有相交日期

- [ ] 10. 增加自然语言快速创建入口
  - 定义自然语言输入到标题、日期、开始时间、结束时间、分类和提醒的解析结果
  - 为解析失败提供可编辑的结构化确认表单
  - 将确认结果复用到日历事件表单和待办排期表单
  - 保持无外部数据能力时的本地解析和安全降级路径
  - 对应统一工具栏快速新增能力和商业化日历扩展方向

- [ ]* 10.1 为自然语言解析编写单元测试
  - 覆盖明确日期、相对日期、时间范围、时长和无效输入
  - 验证解析结果不会生成结束时间早于开始时间的安排

- [ ] 11. 统一 Widget、今日页和日历页的排期刷新
  - 在日历完成、编辑、拖拽改期、清除排期和提醒变更后刷新 Widget
  - 统一今日页与日历页的跨日排期统计规则
  - 保持下一项任务和今日排期时长与日历展示一致
  - 对应现有 App Group、SwiftData 和 Widget 数据流

- [ ]* 11.1 为跨页面排期同步编写集成测试
  - 覆盖日历修改后今日页统计更新
  - 覆盖今日页修改后日历时间线更新
  - 覆盖 Widget 查询到最新完成状态和排期时间

- [ ] 12. 完成日历模块拆分和接口收敛
  - 将 `CalendarHomeView.swift` 中的时间线项、周视图时间槽和日期计算拆分为职责单一的组件或支持类型
  - 收敛事件和待办的统一展示协议，减少重复日期覆盖逻辑
  - 保持现有 SwiftUI 主题、SwiftData 和错误提示模式
  - 对应设计文档 Architecture、Components And Interfaces

- [ ]* 12.1 为日期计算和统一展示模型编写单元测试
  - 覆盖日期覆盖、排序、全天分类、当前时间插入和完成统计
  - 验证视图切换不会改变 `month` 或 `selectedDate`

- [ ] 13. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [ ] 14. 同步日历需求、设计和接口文档
  - 更新 `.monkeycode/specs/calendar-commercial-polish/requirements.md`
  - 更新 `.monkeycode/specs/calendar-commercial-polish/design.md`
  - 更新 `.monkeycode/docs/ARCHITECTURE.md` 和 `.monkeycode/docs/INTERFACES.md`
  - 对应所有已实现需求和设计组件

- [ ]* 14.1 执行 iOS CI 构建验证
  - 运行 `PaperTodo iOS Build`
  - 验证 XcodeGen、无签名构建、IPA 打包和 Artifact 上传

- [ ] 15. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户
