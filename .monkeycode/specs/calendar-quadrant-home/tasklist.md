# 需求实施计划

- [ ] 1. 为待办项添加四象限属性持久化
  - [ ] 1.1 扩展 TodoItem 模型支持象限字段
    - 在 `Models/Paper.swift` 的 `TodoItem` 中新增象限存储属性（默认"未分配"）
    - 定义 `Quadrant` 枚举（重要且紧急/重要不紧急/不重要但紧急/不重要不紧急）及显示名
    - 新属性提供默认值，保持 SwiftData 现有数据兼容，无需迁移
  - [ ] 1.2 实现象限推导与手动分配回退
    - 未手动分配象限的旧任务按文本关键词推导默认象限（复用现有 `quadrantIndex(for:)` 逻辑，R3.AC2）
    - 手动分配后以持久化值为准，保证分类稳定不漂移
  - [ ]* 1.3 编写象限分类单元测试
    - 测试关键词推导规则覆盖各象限
    - 测试手动分配优先级高于推导

- [ ] 2. 检查点 - 确保所有测试通过，如有疑问请询问用户

- [ ] 3. 实现四象限任务操作
  - [ ] 3.1 实现任务移动象限并持久化
    - 在 `QuadrantHomeView` 为任务卡片添加移动菜单，可移到其余三个象限
    - 移动后写入 `TodoItem` 象限属性并保存（R3.AC4）
  - [ ] 3.2 实现任务完成与文本编辑
    - 支持任务完成状态切换并同步保存（R3.AC3）
    - 提供任务文本编辑入口（sheet 或内联编辑）
  - [ ] 3.3 实现打开所属纸片
    - 点击任务跳转到所属 `Paper` 的 `TodoPaperView` 详情（R3.AC3）
  - [ ]* 3.4 编写任务操作集成测试
    - 验证移动象限后重新打开视图分类不变

- [ ] 4. 完善四象限空状态与辅助功能
  - [ ] 4.1 实现空象限新增任务入口
    - 每个空象限显示"新增任务"按钮，新建任务时直接分配到当前象限（R3.AC5）
  - [ ] 4.2 补充象限说明与辅助功能标签
    - 空象限显示当前象限说明文字
    - 为象限卡片、移动菜单、新增按钮添加可读标签（R4.AC2）

- [ ] 5. 检查点 - 确保所有测试通过，如有疑问请询问用户

- [ ] 6. 完善错误处理与多尺寸适配
  - [ ] 6.1 实现保存失败错误提示
    - 四象限任务操作保存失败时显示可理解提示并保留已输入内容（R4.AC3）
  - [ ] 6.2 验证并适配 iPhone/iPad 布局
    - 验证四象限在 iPhone 单列与 iPad 大屏下不重叠、文字不截断、操作可触达（R4.AC1）

- [ ] 7. 优化全局样式与颜色
  - [ ] 7.1 统一全局色彩系统与设计令牌
    - 扩展 `PaperPalette`，补充背景渐变、卡片高亮、主强调色等字段，对齐参考图色彩规范（背景 #F0F4F8→#E8EEF5、主色 #4A7BF7、时间色 #5B9BD5，R5.AC1）
    - 为列表、日历、四象限三个模式建立统一色板映射
  - [ ] 7.2 统一四象限配色
    - 将四象限当前红/橙/蓝/绿替换为与全局设计语言协调的固定色系，保持象限语义可区分（R3.AC1、R4.AC1）
  - [ ] 7.3 统一卡片与控件样式
    - 让列表卡片、四象限卡片与日历卡片共享统一圆角、阴影与材质语言，复用 `PaperRadius` 与 `PaperCardModifier`（R4.AC1）
  - [ ] 7.4 统一全局背景与导航样式
    - 统一首页导航栏、背景渐变与分段/底部导航控件视觉，与日历识图风格协调（R5.AC1）
  - [ ]* 7.5 编写全局样式一致性校验
    - 检查三种模式主色、背景、圆角令牌引用一致

- [ ]* 8. 编写模式切换状态保持测试
  - [ ]* 8.1 测试纸片数据、筛选状态和主题设置跨模式切换保持（R1.AC3）
